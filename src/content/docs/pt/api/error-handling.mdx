---
title: "Tratamento de erros"
description: "RFC 7807 Problem Details — o formato padrão de erros para todas as APIs do Primebrick."
source: handwritten
---

## Visão geral

Primebrick utiliza **RFC 7807 Problem Details for HTTP APIs** como formato universal de erros. Cada erro — do backend e de cada microsserviço — retorna a mesma estrutura JSON. Isso fornece aos clientes uma forma única e previsível de parsear e tratar erros.

## Formato de resposta de erro

Cada resposta de erro é um objeto JSON com os seguintes campos:

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|-------------|-----------|
| `type` | string (URI) | Sim | Um identificador estável do tipo de erro |
| `title` | string | Sim | Um resumo breve e legível por humanos |
| `status` | number | Sim | O código de status HTTP |
| `detail` | string | Não | Uma explicação legível por humanos específica desta ocorrência |
| `instance` | string | Não | O caminho da requisição que provocou o erro |
| `impact` | string[] | Não | Quais elementos ou funcionalidades da UI são afetados (para uso do frontend) |

### Exemplo

```json
{
  "type": "https://primebrick.dev/errors/permission-denied",
  "title": "Permission denied",
  "status": 403,
  "detail": "You do not have permission to read customers.",
  "instance": "/api/v1/customers",
  "impact": ["customers-list", "customer-detail"]
}
```

## Códigos de erro estáveis

O campo `type` é um **código de erro estável** em forma de URI. Ele não muda entre versões para a mesma condição de erro. Os clientes podem fazer switch de forma segura sobre o valor de `type`:

```typescript
switch (error.type) {
  case 'https://primebrick.dev/errors/permission-denied':
    showAccessDeniedBanner();
    break;
  case 'https://primebrick.dev/errors/not-found':
    showNotFoundPage();
    break;
  case 'https://primebrick.dev/errors/validation':
    showValidationErrors(error.detail);
    break;
}
```

## Campo impact

O array opcional `impact` indica ao **frontend quais elementos da UI são afetados** pelo erro. Isso permite que a UI se degrade de forma elegante — desabilitando ou mostrando um estado de erro apenas para os componentes afetados em vez de mostrar um erro global.

```json
{
  "type": "https://primebrick.dev/errors/service-unavailable",
  "title": "Service unavailable",
  "status": 503,
  "detail": "The billing microservice is currently offline.",
  "instance": "/ws/billing/invoices",
  "impact": ["billing-widget", "invoice-export-button"]
}
```

O frontend pode usar isso para mostrar uma mensagem de erro direcionada ao widget de billing enquanto deixa o resto da página funcional.

## Erros comuns

### 400 — Erro de validação

```json
{
  "type": "https://primebrick.dev/errors/validation",
  "title": "Validation error",
  "status": 400,
  "detail": "Field 'email' must be a valid email address.",
  "instance": "/api/v1/customers"
}
```

### 401 — Não autorizado

```json
{
  "type": "https://primebrick.dev/errors/unauthorized",
  "title": "Unauthorized",
  "status": 401,
  "detail": "Authentication is required to access this resource.",
  "instance": "/api/v1/customers"
}
```

### 403 — Permissão negada

```json
{
  "type": "https://primebrick.dev/errors/permission-denied",
  "title": "Permission denied",
  "status": 403,
  "detail": "You do not have permission to delete customers.",
  "instance": "/api/v1/customers/42",
  "impact": ["customer-delete-button"]
}
```

### 404 — Não encontrado

```json
{
  "type": "https://primebrick.dev/errors/not-found",
  "title": "Resource not found",
  "status": 404,
  "detail": "Customer 42 does not exist.",
  "instance": "/api/v1/customers/42"
}
```

### 409 — Conflito

```json
{
  "type": "https://primebrick.dev/errors/conflict",
  "title": "Conflict",
  "status": 409,
  "detail": "A customer with this email already exists.",
  "instance": "/api/v1/customers"
}
```

### 503 — Serviço indisponível

```json
{
  "type": "https://primebrick.dev/errors/service-unavailable",
  "title": "Service unavailable",
  "status": 503,
  "detail": "The billing microservice is currently offline. Please try again later.",
  "instance": "/ws/billing/invoices",
  "impact": ["billing-widget", "invoice-list"]
}
```

## Produzindo erros em código

O SDK fornece um helper `ProblemError` para que os serviços produzam erros conformes de forma consistente:

```typescript
import { ProblemError } from '@primebrick/sdk';

// Erro simples
throw new ProblemError({
  type: 'https://primebrick.dev/errors/not-found',
  title: 'Customer not found',
  status: 404,
  detail: `Customer ${id} does not exist.`,
  instance: `/api/v1/customers/${id}`,
});

// Com impact (para erros com conhecimento do frontend)
throw new ProblemError({
  type: 'https://primebrick.dev/errors/permission-denied',
  title: 'Permission denied',
  status: 403,
  detail: 'You cannot export billing data.',
  instance: '/ws/billing/export',
  impact: ['billing-export-button'],
});
```

## Próximos passos

- [Introdução à API](/pt/api/introduction/) — URLs base e a especificação OpenAPI.
- [Padrão de microsserviços](/pt/api/microservice-standard/) — como os serviços produzem esses erros.
- [API Explorer](/api-explorer/) — veja os erros em ação com requisições em tempo real.
